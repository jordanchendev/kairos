type MetricsViewerProps = {
  metrics: Record<string, number> | null;
};

// Stub — will be replaced in Task 2
export function MetricsViewer(_props: MetricsViewerProps) {
  return <div>No metrics</div>;
}
